import { render } from '@testing-library/react';
import Favourite from './Favourite';

describe('<Favourite />', () => {
  it('should render <Favourite />', () => {
    render(<Favourite />);
  });
});
